# 任务

任务页面按用户目标分组。确切的可运行组合由 `conf/` 下的 owner YAML 支撑，
并在 {doc}`../../5-reference/5-support_matrix` 中汇总。

::::{grid} 1 1 2 2
:gutter: 3

:::{grid-item-card} 运动控制
:link: 1-locomotion
:link-type: doc
XqRobotWL 和 XqRobotV2 行走、粗糙地形、跳跃与足尖行走 owner。
:::

::::

```{toctree}
:hidden:

1-locomotion
```
