# @package _global_
# 消融实验: no_fsm — 移除FSM前馈 (feedback_gain=0, action=纯PPO)
# 继承自 mujoco.yaml 的所有默认值

training:
  task_name: XqRobotWLJumpSRLFlat
  run_suffix: no_fsm

reward:
  feedback_gain: 0.0
  scales:
    wheel_ground_matching: 20.0
